#!/usr/bin/awk -f 
BEGIN{
	print "hello world" 
}



# when we want run awk file like this ./filename then we required above shebang line if not reuired
# if we run our awk file like this awk -f filename then not reuired above shebang line


